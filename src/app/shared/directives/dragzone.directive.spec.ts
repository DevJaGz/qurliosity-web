import { DragzoneDirective } from './dragzone.directive';

describe('DragzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DragzoneDirective();
    expect(directive).toBeTruthy();
  });
});
